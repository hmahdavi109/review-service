server:
  port: 9898

spring:
  datasource:
    url: jdbc:postgresql://review-postgres-db:5432/REVIEW_SERVICE
    username: admin
    password: admin@123
    driver-class-name: org.postgresql.Driver
    initialization-mode: always
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    generate-ddl: true


configuration:
  defaultLocale:
    language: "en"
